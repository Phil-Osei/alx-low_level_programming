#include "main.h"

/**
** _abs - computes the absolute value of an integer
** @n: the int to check
** Return: the absolute value of an int n
*
*/

int abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
		return (-n);
}
